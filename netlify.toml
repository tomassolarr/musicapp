[build]
  command = "pip install -r requirements.txt && python manage.py migrate --noinput"
  publish = "."

[build.environment]
  PYTHON_VERSION = "3.11"

[[redirects]]
  from = "/*"
  to = "/"
  status = 200
